name: 'ORG Checks'
on:
  pull_request:
    types: [opened, edited, synchronize]
jobs:
  org-checks:
    permissions:
      checks: write
      pull-requests: read
    uses: ./.github/workflows/util-pr-checks.yml
